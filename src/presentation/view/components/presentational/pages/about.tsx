import React from "react";
import styled from "styled-components";

export const About = () => {
    return (
        <Title>
            This is About page.
        </Title>
    );
};

const Title = styled.h1`
`;
